# ============================================================
# netlify.toml — Netlify Konfiguration
# The Freescape Project
# ============================================================

[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache"
